#! /usr/bin/perl
#! /usr/bin/perl -i~

{
  while(<>)
    {

      ################################################################################
      #
      # lvalue + rvalue
      #
      ################################################################################

##      s/^( .*> (?<lvalue>
##
##        Bearbeitungs-Nr\.
##
##      ) \s+ (?<rvalue>.*) <\/.* )$/${1}\t\t\t<!-- CAVEAT: {$+{lvalue}}=>{$+{rvalue}} -->/x;

      ################################################################################
      #
      # rvalue only
      # possibly with ":" at the end of the rvalue
      #
      # with placeholder for rvalue
      #
      ################################################################################

      s/^( .*> (?<rvalue>

      	[^<]*

      ) <\/.* )$/${1}\t\t\t<!-- CAVEAT: {}=>{$+{rvalue}} -->/x;

      ################################################################################

      print;
    }
}
